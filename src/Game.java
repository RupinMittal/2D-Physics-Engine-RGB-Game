/**
 * Game - Runs the game
 * This is the head class to run all the game and manage the menu and running of everything in the game
 * @author Team RGB
 * @version May 20 2019
 */

import java.io.FileInputStream;
import java.io.FileNotFoundException;
import javafx.application.Application;
import javafx.scene.Group;
import javafx.scene.Scene;
import javafx.scene.image.Image;
import javafx.scene.image.ImageView;
import javafx.stage.Stage;
import javafx.scene.input.KeyEvent;
import javafx.animation.AnimationTimer;
import javafx.event.EventHandler;

public class Game extends Application
{
    String Character; //the character that the user chooses

    /**
     * Constructor for objects of class Game
     */
    public Game()
    {
    }

    /**
     * This method will be called to play the game
     */
    public void play()
    {
        //make a call to play introduction
        //make call to the menu
        //use menu to obtain the string for character that will be used
        //run sector 1
        //run sector 2
        //run sector 3
    }
}
